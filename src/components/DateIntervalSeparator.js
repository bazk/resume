import React from "react"
import styled from "styled-components"

const Wrapper = styled.span`
  color: #cfcfcf;
  margin: 0 0.25em;
`

const DateIntervalSeparator = () => (
  <Wrapper>&mdash;</Wrapper>
)

export default DateIntervalSeparator